using System;
using System.Collections.Generic;

class Client
{
    public int AccountNumber { get; set; }
    public string ClientName { get; set; }

	public static List<Client> Clients = new List<Client>();

	
	public static void CreateClient()
	{
		int accountNumber = Clients.Count + 1;
		Console.WriteLine("\nInsert name: ");
		string name = Console.ReadLine();
		Clients.Add(new Client { AccountNumber = accountNumber, ClientName = name });
	}
 }

class Stock()
{
	public int StockNumber { get; set; }
	public string StockName { get; set; }
	public double StockPrice { get; set; }
	
	public static List<Stock> Stocks = new List<Stock>();
	
	public static void CreateStock()
	{
		int stockNumber = 0;
		string name = "";
		double price = 0.0;
		Stocks.Add(new Stock { StockNumber = stockNumber, StockName = name, StockPrice = price });
		
		1, TSLA, 20.00
		2, NVDA, 14.00
		3, AMZN, 7.00
		4, AAPL, 12.00
		5, MSFT, 24.00
		6, GOOGL, 30.00

	}
}


class Program
{
    static void Main()
    {
		Client.CreateClient();
		foreach (var client in Client.Clients)
		{
			Console.WriteLine($"Account number: {client.AccountNumber}");
			Console.WriteLine($"Name: {client.ClientName}");
		}
		
		Stock.CreateStock();
	}
}
